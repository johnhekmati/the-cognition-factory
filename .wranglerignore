node_modules/
tailwindcss.exe
terminals/
mcps/
.git/

# Operator docs — versioned in repo, never deployed to the public site.
# NOTE: wrangler pages deploy does NOT honor this file — the actual fence is
# the "Prune non-public files" step in .github/workflows/deploy.yml.
docs/